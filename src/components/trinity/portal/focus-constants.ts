/** Shared timing so the energy-line pulse, island arrival effects, and the
 * click → transition sequence all stay in sync without cross-imports. */
export const FOCUS_TRAVEL_MS = 550;
export const FOCUS_ARRIVE_MS = 420;
export const RETURNING_KEY = "trinity-returning-from";
